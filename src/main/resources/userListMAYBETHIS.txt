<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:th="https://www.thymeleaf.org"
      lang="en">
<head>
    <meta charset="UTF-8">
    <title>Users</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        .container {
            max-width: 90%;
        }
        .table-responsive {
            max-height: 500px;
            overflow-y: auto;
        }
        .sticky-top {
            top: -1px;
            z-index: 1;
        }
        .vertical-btn {
            height: 100vh;
            position: fixed;
            left: 0;
            top: 0;
            width: 60px;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: #f8f9fa;
            border-right: 1px solid #dee2e6;
        }
        .vertical-btn .btn {
            transform: rotate(-90deg);
            white-space: nowrap;
        }
    </style>
</head>
<body>
<div class="container mt-5">
    <h1 class="text-center mb-5">Users List</h1>

    <div class="vertical-btn">
        <a th:href="@{../admin}" class="btn btn-primary">Back to Admin Page</a>
    </div>

    <div class="table-responsive">
        <table class="table table-striped">
            <thead class="sticky-top bg-white">
            <tr>
                <th>ID</th>
                <th>Login</th>
                <th>Password</th>
                <th>Email</th>
                <th>Role</th>
                <th>Deleted</th>
                <th>Action</th>
            </tr>
            </thead>
            <tbody>
            <tr th:each="user, iterStat : ${users}" th:if="${iterStat.index < 10}">
                <td th:text="${user.id}"></td>
                <td th:text="${user.login}"></td>
                <td th:text="${user.password}"></td>
                <td th:text="${user.email}"></td>
                <td th:text="${user.role}"></td>
                <td th:text="${user.deleted}"></td>
                <td>
                    <a th:href="@{users/edit(id=${user.id})}" class="btn btn-sm btn-primary">Edit</a>
                    <a th:href="@{users/delete(id=${user.id})}" class="btn btn-sm btn-danger">Delete</a>
                </td>
            </tr>
            </tbody>
        </table>
    </div>

    <div class="d-flex justify-content-end mt-4">
        <a th:href="@{users/add}" class="btn btn-success">Add New User</a>
    </div>
</div>
</body>
</html>
```

Объяснение изменений:
1. Изменил `overflow-y: scroll` на `overflow-y: auto` для `.table-responsive`, чтобы полоса прокрутки появлялась только при необходимости.
2. Добавил `z-index: 1` для `.sticky-top`, чтобы заголовки таблицы всегда отображались поверх содержимого при прокрутке